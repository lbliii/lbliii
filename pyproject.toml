[project]
name = "lbliii"
version = "0.1.0"
description = "LB's GitHub profile and blog"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "bengal[search]>=0.2.4",
]

[tool.uv.sources]
bengal = { path = "../bengal", editable = true }
